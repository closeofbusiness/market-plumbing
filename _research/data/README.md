Raw downloads only, one subfolder per source (e.g. data/nyfed-fr2004/, data/bis-otcd/).
Keep raw files immutable — never edit in place. Derived series go in data/derived/
with the script that produced them alongside.
