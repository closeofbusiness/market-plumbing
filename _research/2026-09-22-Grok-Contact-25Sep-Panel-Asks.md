# Grok contact note — 25 Sep 2026 (bundle; not a parcel)

**Purpose:** one contact, two artifact asks. Contact is scarcer than the files are urgent. Do **not** open separate parcels for these.

## FIRST, before either ask (supervisor, 23 Sep)

**Confirm `/workspace/tdr/` still exists on the executor.** If that environment has been recycled, the W3 panel is already
gone — and every day it sits only there is a day it can vanish (the d1/d2 class: 33 of 35 scratchpad deliverables were lost
that way). If it is gone, re-pull from SEC companyfacts, which is free and re-fetchable; the scalars in `data/w3_tax/results.json`
are the check. **Land the panels at the first contact that can reach them, whatever the date** — bundling them into 25 Sep
assumed no earlier contact, and there was one on 22 Sep.

## Ask 1 — W3 firm-tax panel (C-097)

Land the 412-firm SEC companyfacts tax panel that backs `2026-09-21-W3-Tax-Decomposition.md` / `data/w3_tax/results.json` into the vault:

- Target: `data/w3_tax/firm_tax_panel.csv` (or equivalent; document schema).
- Must be reachable without the executor box. Scalars already re-derived; this is reproducibility (C-097).
- Optional: raw Z.1 debt pulls named in the W1 rebalance attack → `data/z1_debt/` if not already present.

## Ask 2 — EPS-split firm panel (d3)

Land the per-firm diluted-share panel behind `2026-09-18-EPS-Split.md` / C-093:

- Target: `data/eps_split/panel/` (currently **empty directory**).
- Same defect class as C-097. Mega-coverage + annual share index alone are not enough to reproduce firm Δlogs.

## Same contact, same day — also due

- **QE Sentiment RUN 1** (decays if missed): `Parcel_QE_Sentiment_25Sep_DRAFT_For_Grok.md` FINAL despite filename. Neutral queries; URL-or-drop; TIGHTER/SIMILAR/EASIER vs June. Return `_research/2026-09-25-QE-Sentiment-Run1.md`.
- If budget is tight: **RUN 1 takes precedence** over panel shipping; panels can ride a follow-up message the same day.

## Not this contact

- Goal read-back (principal).
- Singh send (principal).
- W2 progress is separate work already started 22 Sep — do not block panels on W2.
