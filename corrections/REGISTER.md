# Corrections register — moved

**The register of killed claims is [`CORRECTIONS.md`](../CORRECTIONS.md)**, with machine-enforced bans on dead phrasings
(`bash bin/check.sh --all`). For the newest entries, run `bash bin/check.sh --latest`.

This file used to restate selected entries. A second register could only drift, so it is now a pointer (principal’s ruling,
24 September 2026, `THE_ASK.md` E-011). The designs that closed, and why, are in the answer note’s §4.
